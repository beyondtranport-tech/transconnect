// This file is intentionally left blank. The login logic is now handled by the standard /signin page and the middleware.
// The file is kept to prevent Next.js from throwing errors about a deleted file that might be cached in the build process.

export default function AdminLoginPage() {
    return null;
}
