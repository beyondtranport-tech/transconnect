
{
  "entities": {
    "Member": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Member",
      "type": "object",
      "description": "Represents a member of the TransConnect platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Member entity."
        },
        "firstName": {
          "type": "string",
          "description": "The first name of the member."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the member."
        },
        "email": {
          "type": "string",
          "description": "The email address of the member.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The phone number of the member."
        },
        "companyName": {
          "type": "string",
          "description": "The registered business name of the member's transport business."
        },
        "registrationNumber": {
            "type": "string",
            "description": "The official registration number of the company."
        },
        "vatNumber": {
            "type": "string",
            "description": "The VAT registration number of the company."
        },
        "streetAddress": {
          "type": "string",
          "description": "The street address of the company."
        },
        "city": {
          "type": "string",
          "description": "The city of the company's address."
        },
        "province": {
          "type": "string",
          "description": "The province of the company's address."
        },
        "postalCode": {
          "type": "string",
          "description": "The postal code of the company's address."
        },
        "membershipId": {
          "type": "string",
          "description": "Reference to Membership tier ID (e.g., 'professional'). (Relationship: Member 1:1 Membership)"
        },
        "rewardPoints": {
          "type": "number",
          "description": "The number of reward points accumulated by the member for loyalty."
        },
        "walletBalance": {
          "type": "number",
          "description": "The cash balance in the member's wallet, in ZAR."
        },
        "financeApplicationIds": {
          "type": "array",
          "description": "A list of IDs for finance applications submitted by this member.",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
            "type": "object",
            "description": "The server timestamp when the member was created."
        },
        "updatedAt": {
            "type": "object",
            "description": "The server timestamp when the member was last updated."
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "email",
        "phone",
        "companyName",
        "membershipId"
      ]
    },
    "MemberVersion": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MemberVersion",
      "type": "object",
      "description": "A historical snapshot of a Member's data at a specific point in time.",
      "properties": {
        "id": { "type": "string" },
        "firstName": { "type": "string" },
        "lastName": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "phone": { "type": "string" },
        "companyName": { "type": "string" },
        "registrationNumber": { "type": "string" },
        "vatNumber": { "type": "string" },
        "streetAddress": { "type": "string" },
        "city": { "type": "string" },
        "province": { "type": "string" },
        "postalCode": { "type": "string" },
        "membershipId": { "type": "string" },
        "rewardPoints": { "type": "number" },
        "walletBalance": { "type": "number" },
        "financeApplicationIds": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "object" },
        "updatedAt": { "type": "object" }
      }
    },
    "Staff": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Staff",
      "type": "object",
      "description": "Represents a staff member associated with a Member's company.",
      "properties": {
        "firstName": {
          "type": "string",
          "description": "The first name of the staff member."
        },
        "lastName": {
          "type": "string",
          "description": "The last name of the staff member."
        },
        "email": {
          "type": "string",
          "description": "The email address of the staff member.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role or position of the staff member (e.g., Driver, Admin)."
        },
        "memberId": {
          "type": "string",
          "description": "The UID of the parent Member this staff member belongs to."
        },
        "createdAt": {
          "type": "object",
          "description": "The server timestamp when the staff member was added."
        }
      },
      "required": [
        "firstName",
        "lastName",
        "email",
        "role",
        "memberId",
        "createdAt"
      ]
    },
    "Membership": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Membership",
      "type": "object",
      "description": "Represents a single tier of membership plan for the TransConnect platform (e.g., Member, Professional, Enterprise).",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Membership tier (e.g., 'professional')."
        },
        "name": {
          "type": "string",
          "description": "Display name of the membership plan (e.g., Professional)."
        },
        "description": {
          "type": "string",
          "description": "A short description of the ideal user for this plan."
        },
        "price": {
          "type": "number",
          "description": "Monthly price of the membership plan in ZAR. A value of 0 indicates a free plan. A null or undefined value indicates custom pricing."
        },
        "features": {
          "type": "array",
          "description": "A list of features included in this plan.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "name",
        "description"
      ]
    },
    "Contribution": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Contribution",
      "type": "object",
      "description": "Represents a single data contribution from a member.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The UID of the contributing member."
        },
        "type": {
          "type": "string",
          "enum": ["fleet", "supplier", "client"],
          "description": "The type of contribution."
        },
        "data": {
          "type": "object",
          "description": "The submitted data, which varies based on the contribution type."
        },
        "createdAt": {
            "type": "object",
            "description": "The server timestamp when the contribution was made."
        }
      },
      "required": [
        "userId",
        "type",
        "data",
        "createdAt"
      ]
    },
    "FinanceApplication": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Finance Application",
      "type": "object",
      "description": "Represents a finance application submitted by a member through the Finance Mall.",
      "properties": {
        "applicantId": {
          "type": "string",
          "description": "The UID of the member submitting the application."
        },
        "status": {
          "type": "string",
          "enum": ["pending", "under_review", "matched", "rejected", "funded", "membership_payment"],
          "description": "The current status of the application."
        },
        "fundingType": {
          "type": "string",
          "enum": ["asset_finance", "working_capital", "partnership", "credit-top-up", "membership_payment"],
          "description": "The type of funding requested."
        },
        "amountRequested": {
          "type": "number",
          "description": "The amount of funding requested in ZAR."
        },
        "businessDetails": {
          "type": "object",
          "description": "Information about the applicant's business."
        },
        "financials": {
          "type": "object",
          "description": "Financial information, such as turnover and profit."
        },
        "documents": {
          "type": "array",
          "description": "A list of URIs pointing to uploaded supporting documents in cloud storage.",
          "items": {
            "type": "string"
          }
        },
        "createdAt": {
            "type": "object",
            "description": "The server timestamp when the application was submitted."
        },
         "updatedAt": {
            "type": "object",
            "description": "The server timestamp when the application was last updated."
        }
      },
      "required": [
        "applicantId",
        "status",
        "fundingType",
        "amountRequested",
        "createdAt",
        "updatedAt"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Represents a single financial transaction in the member's wallet ledger.",
      "properties": {
        "transactionId": {
          "type": "string",
          "description": "Unique, sequential identifier for the transaction (e.g., R0724-001, P0724-001, ADJ0724-001)."
        },
        "reconciliationId": {
          "type": "string",
          "description": "The ID of the reconciliation batch this transaction belongs to."
        },
        "type": {
          "type": "string",
          "enum": ["credit", "debit"],
          "description": "The type of transaction."
        },
        "amount": {
          "type": "number",
          "description": "The value of the transaction in ZAR."
        },
        "date": {
          "type": "string",
          "format": "date-time",
          "description": "The server timestamp the transaction occurred."
        },
        "description": {
          "type": "string",
          "description": "The raw description from the bank statement or a manual entry."
        },
        "chartOfAccountsCode": {
          "type": "string",
          "description": "The accounting code for this transaction (e.g., '4000-MembershipFees')."
        },
        "status": {
          "type": "string",
          "enum": ["pending_allocation", "allocated", "reversal"],
          "description": "The status of the transaction in the reconciliation process."
        },
        "isAdjustment": {
          "type": "boolean",
          "description": "A flag to identify manual corrections or adjustments."
        }
      },
      "required": ["transactionId", "type", "amount", "date", "description", "status"]
    },
    "Reconciliation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Reconciliation",
      "type": "object",
      "description": "Represents a single bank statement reconciliation batch.",
      "properties": {
        "reconciliationId": {
          "type": "string",
          "description": "Unique identifier for the reconciliation batch (e.g., RECON-2024-07-30)."
        },
        "statementPeriod": {
          "type": "string",
          "description": "The date range covered by the bank statement (e.g., '2024-07-01 to 2024-07-31')."
        },
        "openingBalance": {
          "type": "number",
          "description": "The opening balance from the bank statement."
        },
        "closingBalance": {
          "type": "number",
          "description": "The closing balance from the bank statement."
        },
        "status": {
          "type": "string",
          "enum": ["in_progress", "completed"],
          "description": "The status of the reconciliation process."
        },
        "processedAt": {
          "type": "object",
          "description": "The server timestamp when the reconciliation was completed."
        }
      },
      "required": ["reconciliationId", "statementPeriod", "openingBalance", "status"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/members/{memberId}",
        "definition": {
          "entityName": "Member",
          "schema": {
            "$ref": "#/backend/entities/Member"
          },
          "description": "Stores member profiles. Access is restricted to the authenticated user (path-based ownership).",
          "params": [
            {
              "name": "memberId",
              "description": "The unique identifier of the member, matching the Firebase Auth UID."
            }
          ]
        }
      },
      {
        "path": "/members/{memberId}/versions/{versionId}",
        "definition": {
            "entityName": "MemberVersion",
            "schema": { "$ref": "#/backend/entities/MemberVersion" },
            "description": "Stores historical snapshots of member data.",
            "params": [
                { "name": "memberId", "description": "The UID of the parent member." },
                { "name": "versionId", "description": "The unique ID for the data version." }
            ]
        }
      },
      {
        "path": "/members/{memberId}/staff/{staffId}",
        "definition": {
          "entityName": "Staff",
          "schema": {
            "$ref": "#/backend/entities/Staff"
          },
          "description": "Stores staff members associated with a parent member company.",
          "params": [
            {
              "name": "memberId",
              "description": "The UID of the parent member."
            },
            {
              "name": "staffId",
              "description": "The unique ID for the staff member document."
            }
          ]
        }
      },
      {
        "path": "/members/{memberId}/transactions/{transactionId}",
        "definition": {
          "entityName": "Transaction",
          "schema": {
            "$ref": "#/backend/entities/Transaction"
          },
          "description": "Stores individual financial transactions for a member. Writable by admins, readable by the associated member.",
          "params": [
            {
              "name": "memberId",
              "description": "The UID of the parent member."
            },
            {
              "name": "transactionId",
              "description": "The unique identifier of the financial transaction."
            }
          ]
        }
      },
      {
        "path": "/memberships/{membershipId}",
        "definition": {
          "entityName": "Membership",
          "schema": {
            "$ref": "#/backend/entities/Membership"
          },
          "description": "Stores details of available membership plans (tiers), readable by all authenticated users.",
          "params": [
            {
              "name": "membershipId",
              "description": "The unique identifier of the membership plan (e.g. 'professional')."
            }
          ]
        }
      },
      {
        "path": "/contributions/{contributionId}",
        "definition": {
          "entityName": "Contribution",
          "schema": {
            "$ref": "#/backend/entities/Contribution"
          },
          "description": "Stores anonymous data contributed by members across different categories (fleet, suppliers, etc.). Writable by any authenticated user, readable by admins.",
          "params": [
            {
              "name": "contributionId",
              "description": "The unique identifier of the contribution document."
            }
          ]
        }
      },
      {
        "path": "/financeApplications/{applicationId}",
        "definition": {
          "entityName": "FinanceApplication",
          "schema": {
            "$ref": "#/backend/entities/FinanceApplication"
          },
          "description": "Stores finance applications submitted by members. Writable by the applicant, readable by admins and matched financiers.",
          "params": [
            {
              "name": "applicationId",
              "description": "The unique identifier of the finance application."
            }
          ]
        }
      }
    ],
    "reasoning": "This Firestore data structure is designed to support the TransConnect platform, focusing on membership management and providing a foundation for future divisions. The structure emphasizes authorization independence, clarity, and scalability.\n\n**Authorization Independence and QAPs:**\n\n*   **Members:** Member data is stored under the `/members/{memberId}` path.  Access is restricted to the authenticated user (path-based ownership). The `members` collection supports secure `list` operations because each document's access control is self-contained within the path and authentication context.\n*   **Memberships:** Membership plans are stored in the `/memberships/{membershipId}` collection.  These are global and are read-only for all authenticated users. Secure `list` operations are enabled because there are no authorization dependencies on other collections.\n\n**Explanation of Structure and Design Choices:**\n\n*   **/members/{memberId}**: This collection stores individual member profiles. Using the user's UID as the document ID ensures that only the authenticated user can access their own profile data, providing strong path-based ownership. It now includes a `financeApplicationIds` array to securely link a member to their financial transactions without requiring insecure collection queries.\n*   **/members/{memberId}/versions/{versionId}**: This subcollection stores historical snapshots of the parent member document. It is append-only for the owning user, providing a complete audit trail of changes. Only admins can read the full history.\n*   **/members/{memberId}/staff/{staffId}**: This new subcollection will store staff members for a specific company (Member). The parent member will have full CRUD access over their own staff, ensuring data ownership and security.\n*   **/members/{memberId}/transactions/{transactionId}**: By moving transactions to a subcollection under each member, we create a secure, scalable, and query-efficient data model. A user can only access transactions within their own document path, making security rules simple and robust.\n*   **/memberships/{membershipId}**: Stores the details of each membership plan. This collection can be accessed by all authenticated users to view available plans. No write access is granted to standard users.\n*   **/contributions/{contributionId}**: This unified collection will store all data submitted through the Contribution Hub. A `type` field within each document will differentiate between fleet data, supplier data, etc. Any authenticated user can create (contribute) data. To maintain security and control, only administrators will have read (list) access to view all contributions in the backend portal. This prevents members from seeing each other's raw data submissions. The `contributionId` will be auto-generated by Firestore, ensuring each submission is unique.\n*   **/financeApplications/{applicationId}**: This collection will store all applications submitted through the Finance Mall. An applicant can create and update their own application. Only administrators (and eventually, matched financiers) will have read access. This ensures the sensitive financial data within an application is kept private and secure.\n\nThis structure provides a solid foundation for the TransConnect platform, adhering to the principles of authorization independence, clarity, and scalability. It supports secure `list` operations and enables robust security rules."
  }
}

    